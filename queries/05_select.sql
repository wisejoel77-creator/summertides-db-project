- Displays the artist directory.
SELECT artist_name, genre, country
FROM artists;

-- Displays the available ticket types and their prices.
SELECT ticket_name, price, access_description
FROM ticket_types;

-- Displays all festival stages and their capacities.
SELECT stage_name, stage_type, capacity
FROM stages;
